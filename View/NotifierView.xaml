<base:NotifierViewBase x:Class="WPF_StatusNotification.View.NotifierView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:base="clr-namespace:WPF_StatusNotification.Base"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_StatusNotification.View"
        mc:Ignorable="d"
        Title="NotifierView" Height="150" Width="300" AllowsTransparency="True" Background="#FF131212" Opacity="0.8" WindowStyle="None">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <!--<Grid Margin="20">
                <Grid.Background>
                    <ImageBrush></ImageBrush>
                </Grid.Background>
            </Grid>-->
            <Grid Margin="20">
                <Button Background="Transparent">
                    <Image Opacity="{Binding Path=Opacity,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type base:NotifierViewBase}}}"></Image>
                </Button>
            </Grid>

        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid Margin="20">
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,0" FontSize="22" FontWeight="Bold" Foreground="White" >Title</TextBlock>
            </Grid>
            <Grid Margin="20" Grid.Row="1">
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap"></TextBlock>
            </Grid>
        </Grid>

    </Grid>
</base:NotifierViewBase>
